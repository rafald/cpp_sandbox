sudo: required
dist: trusty
language: cpp
compiler:
#  - clang-3.9
#  - gcc-4.9
  - gcc
env:
global:
  - CTEST_OUTPUT_ON_FAILURE=1
matrix:
  - PROJECT=CPP_SANDBOX

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - ppa:purplekarrot/cmake
    packages:
    - g++-4.9
    - g++-5
    - cmake
    - cmake-data
    - libopenmpi-dev

before_install:
#  - sudo apt-get install libboost-tools-dev 
#  - mkdir build
  - ./shallow-clone master https://github.com/boostorg/boost.git boost
  - ls boost/tools/build/src
  - ls ~/build/rafald/cpp_sandbox/boost/tools/build/src
  - which gcc
#
#  - echo "using gcc : 4.9 ; " >> ~/build/rafald/cpp_sandbox/boost/tools/build/src/user-config.jam
#$ {:"echo \"using gcc"=>"4.9 ; \" >> ./boost/tools/build/src/user-config.jam"}
#/home/travis/build.sh: line 45: 4.9 ; " >> ./boost/tools/build/src/user-config.jam}: No such file or directory

#  - echo "using gcc : 4.4 : /usr/bin/g++-4.4 ; " >> tools/build/src/user-config.jam
#  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
#  - sudo apt-get update -q
#  - sudo apt-get install gcc-4.9 -y
  - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.9 20
  - sudo update-alternatives --config gcc
  - which gcc
#  - ~/build/rafald/cpp_sandbox/boost/bootstrap.sh --with-toolset=gcc-4.9

# skips install
#install: true
#install: ./install-dependencies.sh
install: 

before_script:
  - ${CC} --version
  - bash -c "echo 'using gcc : 4.9 : /usr/bin/gcc-4.9 ; ' >> ~/build/rafald/cpp_sandbox/boost/tools/build/src/user-config.jam"
  - bash -c "echo 'using gcc : 5 : /usr/bin/gcc-5 ; ' >> ~/build/rafald/cpp_sandbox/boost/tools/build/src/user-config.jam"
  - cat ~/build/rafald/cpp_sandbox/boost/tools/build/src/user-config.jam
  - pwd
  - pushd boost ; ./bootstrap.sh --with-toolset=gcc

script:
  - ${CC} --version
  - touch Jamroot
  - pwd
  - BOOST_ROOT=./boost ./boost/b2 -d2 toolset=${CC}-4.9 
#  - cd build
#  - cmake -DCMAKE_BUILD_TYPE=Debug ..
#  - cmake --build . --target $PROJECT-test

after_success:
  - bash -c "echo 'RLD: SUCCESS'"

after_failure:
  - bash -c "echo 'RLD: failed\!'"
  